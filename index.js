#!/usr/bin/env node

/**
 * Main entry point for Dia-Moderator application
 * This file bootstraps the server
 */

// Import server module
const server = require('./server/server');

// Application is launched from server.js directly
console.log('Dia-Moderator starting up...');